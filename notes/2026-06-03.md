1. 操作：eth存款交易尝试，将账户的eth存入测试网Aave合约
2. 遇到的问题：
  2.1 第一步Aave存款的ABI接口地址无法获取，查看https://sepolia.etherscan.io/address/0x6Ae43d3271ff6888e7Fc43Fd7321a503ff738951测试交易网并没有找到存款的ABI接口地址，最后才发现一直查看的是Aave代理合约的地址所以找到的ABI没有deposit和supply方法，查看实施(Implementation)合约的地址https://sepolia.etherscan.io/address/0x0562453c3dafbb5e625483af58f4e6d668c44e19就能找到对应的ABI接口方法了
  2.2 刚开始一直使用的deposit()方法，后面才发现这个方法是Aave v2版本使用的存款方法，Aave v3版本应该使用是supply()方法进行存款
  2.3 刚开始存款一直不成功，还有个原因是设置了gaslimit，但是设置的gaslimit过小gas: 100000n, 导致交易失败，最后没有设置gas了等交易自动计算gas存款成功了
3. 遗留问题：之前存款失败的eth金额交易应该是回滚了，但是目前账户里面没有这部分eth，失败这部分金额跑去哪里了？

1. Task: Attempt to deposit ETH into the Aave contract on the testnet
2. Issues encountered:
  2.1 The ABI interface address for Aave deposit cannot be obtained. Upon checking https://sepolia.etherscan.io/address/0x6Ae43d3271ff6888e7Fc43Fd7321a503ff738951, the test transaction network, the ABI interface address for deposit was not found. It was eventually discovered that the address being viewed was that of the Aave proxy contract, hence the ABI found did not include deposit and supply methods. By checking the implementation contract address https://sepolia.etherscan.io/address/0x0562453c3dafbb5e625483af58f4e6d668c44e19, the corresponding ABI interface method could be found. 2.2 Initially, the deposit() method was used, but later it was discovered that this method was the deposit method used in Aave v2 version. The supply() method should be used for deposit in Aave v3 version
  2.2 Initially, I kept using the `deposit()` method, but later realised that this method is used for deposits in Aave v2; in Aave v3, the `supply()` method should be used for deposits
  2.3 Another reason why deposits kept failing initially was that I had set the gas limit too low (gas: 100000n), causing the transaction to fail. Eventually, I removed the gas limit and the deposit succeeded once the gas was calculated automatically.
3. Outstanding issue: The transaction for the failed deposit should have been rolled back, but this amount of ETH is currently missing from the account. Where has this failed amount gone?

Translated with DeepL.com (free version)